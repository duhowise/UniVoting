<ui:FluentWindow x:Class="UniVoting.Admin.MainWindow"
                      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                      xmlns:local="clr-namespace:UniVoting.Admin"
                      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                      mc:Ignorable="d"
                      WindowState="Maximized"
                      WindowBackdropType="Mica"
                      Title="UNIVOTING" Height="350" Width="525">
    <Window.Resources>
        <ControlTemplate x:Key="ControlTemplate" TargetType="Frame">
            <DockPanel>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Top">
                    <ui:Button Appearance="Primary" Width="36" Height="36"
                               Command="{x:Static NavigationCommands.BrowseBack}">
                        <ui:SymbolIcon Symbol="ArrowLeft24" />
                    </ui:Button>
                    <ui:Button Appearance="Primary" Width="36" Height="36"
                               Command="{x:Static NavigationCommands.BrowseForward}">
                        <ui:SymbolIcon Symbol="ArrowRight24" />
                    </ui:Button>
                </StackPanel>
                <ContentPresenter></ContentPresenter>
            </DockPanel>
        </ControlTemplate>
        <Style x:Key="PageFrage" TargetType="Frame">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Frame">
                        <DockPanel>
                            <StackPanel Orientation="Horizontal" DockPanel.Dock="Top">
                                <ui:Button Appearance="Primary" Width="36" Height="36"
                                           Command="{x:Static NavigationCommands.BrowseBack}">
                                    <ui:SymbolIcon Symbol="ArrowLeft24" />
                                </ui:Button>
                            </StackPanel>
                        </DockPanel>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid>
        <Frame Name="PageHolder" Template="{StaticResource ControlTemplate}"/>
    </Grid>
</ui:FluentWindow>
