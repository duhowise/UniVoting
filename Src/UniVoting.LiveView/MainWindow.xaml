<Window x:Class="UniVoting.LiveView.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UniVoting.LiveView"
        mc:Ignorable="d"
        WindowState="Maximized"
        Title="UNIVOTING">
    <ContentControl>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>
            <Label FontSize="16" Grid.Column="0" Grid.Row="0"
                   FontFamily="Segoe UI Bold" Margin="0"
                   Foreground="{DynamicResource SystemAccentColorBrush}">Casted Votes</Label>
            <UniformGrid Grid.Column="0" Grid.Row="1" Name="CastedVotesHolder"></UniformGrid>
            <Label FontSize="16" Grid.Column="1" Grid.Row="0"
                   FontFamily="Segoe UI Bold" Margin="0"
                   Foreground="{DynamicResource SystemAccentColorBrush}">Skipped Votes</Label>
            <UniformGrid Grid.Row="1" Grid.Column="1" Name="SkippedVotesHolder"></UniformGrid>
        </Grid>
    </ContentControl>
</Window>
